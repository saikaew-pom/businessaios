# BusinessAiOs — UX Audit & Redesign Plan

> ⚠️ **แทนที่แล้วโดย `OS_REDESIGN_MASTER_PLAN.md` (ส.ค. 2569)** — audit/ประเด็น P0-P2 ในไฟล์นี้ยังเป็นข้อเท็จจริงอ้างอิงได้ แต่แผนการสร้างให้ยึดตาม master plan
> จัดทำโดย: UX/UI specialist (take-over review) · 2026-07-29
> ผู้รับงานต่อ: UI designer specialist → implementation phase
> สถานะ: **plan + wireframe เท่านั้น — ยังไม่แตะโค้ด UI จริง** (implement ในเฟสถัดไป)

---

## 0. วิธีรีวิว & เกณฑ์ (The Test)

รีวิวทั้งระบบที่ live จริง (`businessaios-web.pskspace.workers.dev`) ผ่านสายตา **persona ที่โหดที่สุด**:

> **"เด็ก ป.6 ที่อยากเปิดธุรกิจ ใช้เองโดยไม่มีใครสอน"**

เกณฑ์ผ่าน = เด็กคนนี้ต้อง (1) เข้าใจว่านี่คืออะไรใน 5 วินาที, (2) รู้ว่าต้องกดปุ่มไหนก่อน โดยไม่ต้องเดา, (3) ทำจนได้ผลลัพธ์ชิ้นแรกโดยไม่หลง, (4) ไม่เจอศัพท์ที่ไม่เข้าใจจนต้องหยุด

surface ที่รีวิว: Landing → Register → Dashboard → Wizard 7 ขั้น → Billing → Tools → Profile → Admin → Mobile

**บทสรุป 1 บรรทัด:** ระบบ *ทำงานได้* และมีของเยอะมาก แต่ **พูดภาษาของนักการตลาด MBA ไม่ใช่ภาษาของ SME มือใหม่** — เด็ก ป.6 จะตกที่ "ต้องเลือกอะไรก่อน" ตั้งแต่หน้า dashboard และเจอศัพท์อังกฤษ/jargon จนหยุดกลางทาง ปัญหาใหญ่สุดไม่ใช่ความสวย แต่คือ **information architecture + ภาษา + การไม่มี onboarding**

---

## 1. ประเด็นที่เจอ (จัดตาม severity)

### 🔴 P0 — บล็อกไม่ให้ผู้ใช้มือใหม่ไปต่อได้

**P0-1 · ไม่มี onboarding เลย + 4 จุดเริ่มต้นแข่งกันบน Dashboard**
หลัง register เด็กเด้งเข้า dashboard ทันที เจอ 4 CTA พร้อมกัน: `+ โปรเจกต์ใหม่`, `⚡ เครื่องมือ AI`, banner สีน้ำเงิน `เครื่องมือ AI แยกต่างหาก`, และการ์ด `Strategic Tools` — **ไม่มีอะไรบอกว่า "เริ่มตรงนี้"** เด็กไม่รู้ความต่างระหว่าง "โปรเจกต์" กับ "เครื่องมือ" → หยุดคิดตั้งแต่ก้าวแรก
*ผลกระทบ:* drop-off สูงสุดอยู่ตรงนี้ · *แก้:* first-run onboarding + เลือก "เริ่มต้นทางเดียว" ที่ชัด (ดู §2.2)

**P0-2 · Landing page โฆษณาสิ่งที่ระบบไม่มีจริง (ทำลายความเชื่อใจ)**
- โฆษณา "AI 4 ตัว: **GPT-4o, Claude, Gemini, Llama** เลือกตามงบ" → ระบบจริงใช้ **MiniMax M3 ตัวเดียว**
- โฆษณา "**BYOK** ใช้ API key ตัวเอง จ่ายตรง provider ไม่มี markup" → ไม่ใช่ default (มีเป็น feature ซ่อน)
- ราคา Landing = **Free 0 / Pro 499 / Team 1,999 บาท ต่อเดือน (subscription)**
  ราคา Billing จริง = **Starter ฿99 / Popular ฿249 / Pro ฿399 (credit แบบซื้อครั้งเดียว, PromptPay)**
  → **คนละโมเดลราคากันคนละเรื่อง** คนที่สมัครเพราะ "Pro 499 ได้ GPT-4o" จะงงเมื่อเข้า billing เจอราคาคนละชุด ไม่มีให้เลือกโมเดล
*ผลกระทบ:* trust พังทันทีที่ user จ่ายเงิน · *แก้:* เขียน landing ใหม่ให้ตรงระบบจริง (§2.1)

**P0-3 · Admin console จัดการ 1,000 users ไม่ได้จริง**
- `adminListUsers` ดึงแค่ `LIMIT 200` + **ไม่มี search / filter / pagination** → พอมี user เกิน 200 คน แอดมิน *มองไม่เห็น* ที่เหลือ และหา user รายคนไม่ได้เลย
- **ไม่มีเครื่องมือ "ช่วย user"** ตามที่ตั้งเป้า: เปิดดู project/generation ของ user ไม่ได้, resend verification email ไม่ได้, reset password ให้ไม่ได้, ดู activity log รายคนไม่ได้, ไม่มี "view as user"
- เปลี่ยน role เป็น admin ได้ทันทีจาก dropdown **ไม่มี confirm** (อันตราย)
*ผลกระทบ:* บล็อกเป้า 1,000 active users ปี 2027 โดยตรง · *แก้:* redesign admin เป็น operations console (§2.6)

### 🟠 P1 — ทำให้สับสน/หลง แต่ยังพอไปต่อได้

**P1-1 · ภาษาปนกันมั่ว + ศัพท์ที่มือใหม่ไม่รู้จัก**
- Wizard: ชื่อ step เป็น **อังกฤษล้วน** ("Business DNA", "Customer Persona", "Customer Journey", "Positioning", "KPI Dashboard") แต่ field ข้างในเป็นไทย → เด็กอ่าน tab ไม่ออก
- ศัพท์ที่ไม่แปล/ไม่มีคำอธิบาย: **"Pain Point", "Business DNA", "Persona", "Brand Voice", "JTBD", "VPC", "BYOK", "Strategic Tools", "tool chain"** — เด็ก ป.6 (และ SME ส่วนใหญ่) ไม่รู้จัก
- Dashboard subtitle เขียน "สร้าง **Marketing System** ด้วย AI" — คำว่า Marketing System ยังนามธรรมเกินไป
*แก้:* บังคับ locale ไทยเป็น default + แปล/อธิบายศัพท์ทุกจุด + ใส่ tooltip "นี่คืออะไร" (§2.3)

**P1-2 · Tools page พูดภาษา MBA**
คำอธิบายเครื่องมือเต็มไปด้วย framework ศัพท์สูง: *"framework จาก McKinsey, Dan Roam, Phil Waknell"*, *"Value Equation + 7 Components + MAGIC naming"*, *"SCQA / 5M / Pop-Up"*, *"LAER + Reframing"* — เด็กไม่มีทางรู้ว่าเครื่องมือแต่ละตัว *ช่วยอะไรฉันได้* · *แก้:* เขียนใหม่เป็น "ช่วยคุณ___" ภาษาคน (§2.4)

**P1-3 · Mobile header พัง**
บนจอมือถือ (375px) header ล้น — "ออกจากระบบ" กับ email หลุดขอบจอ (logout กดไม่ได้), "เติมเงิน" ตัดขึ้นบรรทัดใหม่, **ไม่มี hamburger menu** · *แก้:* mobile nav ใหม่ (bottom tab bar หรือ hamburger) (§2.5)

**P1-4 · "+ โปรเจกต์ใหม่" เปิดฟอร์ม inline ที่ท้ายหน้า (ต่ำกว่าจอ)**
กดปุ่มมุมขวาบน แต่ฟอร์มไปโผล่ล่างสุดใต้ fold — เด็กกดแล้ว "เอ๊ะ ไม่มีอะไรเกิดขึ้น" เพราะไม่เห็นว่าฟอร์มโผล่ที่ไหน · *แก้:* modal หรือหน้า dedicated + auto-scroll/focus

### 🟡 P2 — ความสม่ำเสมอ/ความน่าเชื่อถือของงานดีไซน์

- **P2-1 · ไอคอนไม่เป็นระบบ:** landing ใช้ `⚡` ซ้ำทุก feature (6 อันเหมือนกันหมด), tools ใช้ emoji สุ่ม (ไมค์ 🎤, เพชร 💎, โล่ 🛡️, เบ็ดตกปลา 🎣, กราฟ 📊) — ดูไม่โปร ควรใช้ icon set เดียวกันทั้งระบบ
- **P2-2 · badge "NEW" ติดทุกการ์ด** → ถ้าทุกอย่าง NEW ก็ไม่มีอะไร NEW (คำเตือนหมดความหมาย)
- **P2-3 · ตัวเลข credit ขัดกันเอง:** สมัครใหม่ระบบให้ "100 credits" (แสดงบน UI) แต่หน้า billing เขียน "Free tier = 200 credits" — เป็นบั๊กจริงที่ user เห็น (backend log signup bonus 200 แต่ user row ตั้ง 100)
- **P2-4 · sticky header ทับ content** เวลา scroll (เห็น header ลอยมากลางหน้า) — visual bug
- **P2-5 · design token จำกัด:** มีสีหลักสีเดียว (electric blue) + neutral scale กระโดด (dark 200 → 900 ไม่มีตรงกลาง), ไม่มี semantic color (success/warning/danger) เป็นระบบ, ไม่มี dark mode — ทำให้ redesign ขาด vocabulary
- **P2-6 · error ใช้ `alert()`** ทั้งระบบ (admin, tools) — ดูเก่า, ไม่ inline
- **P2-7 · a11y:** ปุ่มเลือก "kind" ตอนสร้างโปรเจกต์ไม่มี label (accessibility tree อ่านไม่ออก), การ์ด/ปุ่มหลายจุดพึ่ง emoji แทนข้อความ

### ✅ ของที่ทำได้ดีอยู่แล้ว (เก็บไว้)
- **Billing page** — การ์ดราคาสะอาด, ต่อ credit ชัด, badge ประหยัด %, ขั้นตอน PromptPay 1-2-3 (หน้าที่ดีที่สุดในระบบ)
- Register form สั้น กระชับ ขอแค่ที่จำเป็น
- Profile page ครบ (avatar, credit history, เปลี่ยนรหัส, 2FA)
- Wizard มี progress ชัด ("STEP 1 OF 7") + placeholder ยกตัวอย่างจริง ("เช่น ก๋วยเตี๋ยวบ้านหมู")
- Palette electric-blue + Noto Sans Thai อ่านง่าย เป็นฐานที่ต่อยอดได้

---

## 2. แผน Redesign (ส่งต่อ UI Designer)

### หลักการออกแบบ 5 ข้อ (Design Principles)
1. **ภาษาคน ไม่ใช่ภาษาเอเจนซี** — ทุกคำที่ user เห็นต้องเป็นสิ่งที่แม่ค้าตลาดนัดเข้าใจ ศัพท์เทคนิคซ่อนไว้ใต้ "(ดูเพิ่ม)"
2. **ทางเดียวที่ชัด (One obvious next step)** — ทุกหน้ามี CTA หลัก *หนึ่งเดียว* ที่เด่นสุด ที่เหลือเป็นรอง
3. **สอนระหว่างทาง (Teach in context)** — อธิบายศัพท์ตรงจุดที่เจอ ด้วยตัวอย่าง ไม่ใช่หน้าช่วยเหลือแยก
4. **มือถือมาก่อน (Mobile-first)** — ผู้ใช้ไทย 1,000 คนส่วนใหญ่มาจากมือถือ
5. **จริงใจเรื่องราคา/AI** — บอกตรงๆ ว่าใช้ AI อะไร ราคาเท่าไร ไม่ over-promise

### 2.1 Landing Page
- เขียน hero ใหม่ให้ตรงระบบ: ไม่พูด "AI 4 ตัว/BYOK" ถ้าไม่ใช่ default — พูดสิ่งที่ทำได้จริง: *"ตอบไม่กี่คำถาม ได้แผนการตลาดครบชุด พร้อมใช้"*
- ราคาบน landing = ชุดเดียวกับ billing (Starter/Popular/Pro credit packages) + อธิบาย "credit คืออะไร" ("1 ครั้งที่ AI ช่วยคิด ≈ กี่ credit")
- แทน `⚡` ซ้ำๆ ด้วย icon set เดียวกัน (แนะนำ Lucide/Phosphor) 1 ไอคอน/1 feature
- เพิ่ม "ผลลัพธ์หน้าตาเป็นยังไง" — โชว์ตัวอย่าง Brand Card/Content Calendar จริง (visual proof) แทนการบรรยาย

### 2.2 Onboarding + Dashboard (แก้ P0-1)
- **First-run flow (3 จอ):** หลัง verify email → (1) "คุณอยากทำอะไรวันนี้?" เลือก 1 ใน 2 การ์ดใหญ่: **"สร้างแผนธุรกิจครบชุด (45 นาที)"** vs **"ลองเครื่องมือเดี่ยวเร็วๆ (5 นาที)"** → (2) ถ้าเลือกครบชุด พาเข้า wizard เลย; ถ้าเลือกเดี่ยว โชว์ 3 เครื่องมือยอดฮิต → (3) tooltip ชี้ที่ credit + ปุ่ม
- **Dashboard ใหม่:** ตัดจาก 4 CTA เหลือ **1 primary** ("+ สร้างแผนใหม่") + ส่วน "เครื่องมือเดี่ยว" เป็น section รอง ไม่ใช่ banner แข่ง · ยุบ "Strategic Tools" + "เครื่องมือ AI แยกต่างหาก" ที่ซ้ำซ้อนให้เหลืออันเดียว
- Empty state พูดชวน: *"ยังไม่มีแผน — มาสร้างแผนแรกกัน กดปุ่มสีน้ำเงินด้านบน"* + ลูกศรชี้

### 2.3 Wizard 7 ขั้น (แก้ P1-1)
- ชื่อ step เป็น **ไทย + เลข + ไอคอน**: 1️⃣ ตัวตนธุรกิจ · 2️⃣ ลูกค้าของเรา · 3️⃣ เส้นทางลูกค้า · 4️⃣ จุดขาย · 5️⃣ ปฏิทินคอนเทนต์ · 6️⃣ ขั้นตอนทำงาน · 7️⃣ วัดผล (KPI) — เก็บชื่ออังกฤษไว้เป็น subtitle เล็กๆ ได้
- แต่ละ field ที่มีศัพท์ ใส่ helper 1 บรรทัด + ตัวอย่าง: *"Pain Point = ปัญหาที่ลูกค้าเจอ เช่น 'หาของกินสายที่เปิดดึกไม่ได้'"*
- Step bar ที่ล้นจอ (7 ขั้นตัด "KPI D...") → บนมือถือทำเป็น dot/progress + ชื่อ step ปัจจุบันเท่านั้น
- ปุ่ม "✨ Generate" ต้องบอกล่วงหน้าว่า **ใช้กี่ credit** ("ใช้ ~3 credit") ก่อนกด

### 2.4 Tools (แก้ P1-2)
- ชื่อเครื่องมือ + คำอธิบายเป็น "ช่วยคุณ___" ภาษาคน:
  *Million Dollar Offer → "ออกแบบข้อเสนอที่ลูกค้าปฏิเสธยาก"* (ซ่อน "Value Equation/MAGIC" ใต้ "ดูหลักการ")
  *Objection Handler → "เตรียมคำตอบเวลาลูกค้าต่อรอง/ลังเล"*
  *Hook Library → "หาประโยคเปิดที่คนหยุดอ่าน"*
- ตัด badge "NEW" ออกจากทุกอัน (เก็บไว้ติดเฉพาะของใหม่จริง)
- icon set เดียว, จัดหมวด (คิดกลยุทธ์ / เขียนคอนเทนต์ / ขาย) แทนกองรวม

### 2.5 Mobile (แก้ P1-3)
- **Bottom tab bar** (มือถือ): หน้าแรก / เครื่องมือ / เครดิต / โปรไฟล์ — เข้าถึงด้วยนิ้วโป้ง
- ย้าย logout + email เข้าไปในหน้า Profile/เมนู ไม่ให้ล้น header
- ทุกปุ่มหลัก ≥ 44px, แตะง่าย

### 2.6 Admin → "Operations Console" (แก้ P0-3, รองรับ 1,000 users + ช่วย user)
โครงใหม่ 4 โซน:
1. **Overview** — การ์ดสถิติ + กราฟ signups รายวัน + funnel (สมัคร → verify → สร้างแผน → จ่ายเงิน) + ต้นทุน AI เทียบรายได้
2. **Users (ตัวหลัก)** — ตาราง + **search (email/ชื่อ) + filter (role, verified, มี/ไม่มี credit, active ล่าสุด) + pagination/infinite scroll** (ต้องแก้ API ให้รับ `?q=&page=&filter=` แทน LIMIT 200 ตายตัว) · แต่ละแถวกด → **User Detail**
3. **User Detail (ของใหม่ — หัวใจของ "ช่วย user")** — ดูโปรไฟล์, credit + ประวัติ transaction, project/generation ทั้งหมดของ user, ปุ่ม: เติม/หัก credit (มี confirm), **resend verification email**, **ส่งลิงก์ reset password**, **"ดูในมุมของ user" (impersonate อ่านอย่างเดียว)**, note ภายในทีม
4. **Support/Email** — outbox + สถานะส่ง (มีอยู่แล้ว ต่อยอด)
- เพิ่ม confirm dialog ทุก action ที่ย้อนไม่ได้ (เปลี่ยน role, หัก credit) · แทน `alert()` ด้วย toast/inline

### 2.7 MCP Server (ของใหม่ที่ต้องมี — ให้ Claude Code users ต่อเข้าระบบ)
เป้าหมาย: คนที่ใช้ Claude Code / Claude Desktop เรียกใช้ความสามารถของ BusinessAiOs ผ่าน MCP ได้ (เหมือนที่โปรเจกต์นี้ต่อ Carta/Supermetrics ฯลฯ)
- **Auth:** ออก **Personal Access Token / MCP key** ต่อ user (หน้า Profile → "เชื่อมต่อ Claude Code" → generate token + คู่มือ config) — reuse ระบบ BYOK/credit เดิม (เรียกผ่าน MCP ก็หัก credit เดียวกัน)
- **Tools ที่ expose (เฟสแรก):** `create_project`, `generate_step`, `run_tool` (10 เครื่องมือ), `list_projects`, `get_export`, `check_credits` — map ตรงกับ REST endpoints ที่มีอยู่แล้ว
- **UX ที่ต้องออกแบบ:** หน้า "Developers / MCP" (การ์ดคัดลอก config JSON สำหรับ `claude mcp add`, สถานะ token, usage/credit ที่ใช้ผ่าน MCP, ปุ่ม revoke) + ทำให้ค้นเจอจาก nav
- อ้างอิงแนวทาง studio.zenityx.com (reference ที่ user ให้) สำหรับ look & feel ของ developer surface
- *หมายเหตุ:* เป็น requirement ใหม่ ไม่มีในระบบปัจจุบันเลย — ต้องออกแบบทั้ง token model + UI + เขียน MCP server (เฟส implement)

### 2.8 Design System (ยกระดับ token — แก้ P2-5)
ให้ designer นิยาม:
- **สีหลัก** electric-blue (เก็บ) + **neutral scale เต็ม** (50→900 ไม่กระโดด) + **semantic** success/warning/danger/info เป็นระบบ
- **Dark mode** (ตั้งแต่ต้น ไม่ retrofit)
- **Type scale** ไทย/อังกฤษคู่ (Noto Sans Thai + Inter) — กำหนด line-height ที่พอดีกับสระบน/ล่างของไทย
- **Icon set เดียว** (Lucide หรือ Phosphor) แทน emoji ทั้งระบบ
- **Component library:** button (primary/secondary/ghost/danger), input+helper+error, card, modal, toast, table, tabs, badge, empty-state, credit-pill, step-indicator — ทำเป็น Svelte component reuse ได้ (ตอน implement)

---

## 3. ลำดับงานเฟส Implement (proposal — รอ user เคาะ)

| เฟส | สิ่งที่ทำ | เหตุผลจัดลำดับ |
|---|---|---|
| **A. Design System + Onboarding + Dashboard** | token, component library, first-run flow, dashboard 1-CTA | ฐานของทุกหน้า + แก้ drop-off จุดใหญ่สุด (P0-1) |
| **B. ภาษา + Landing + Wizard + Tools** | locale ไทย default, แปลศัพท์, เขียน copy ใหม่, mobile nav | แก้ P0-2/P1-1/P1-2/P1-3 — เนื้อหาที่ user เห็นบ่อยสุด |
| **C. Admin Operations Console** | search/filter/pagination + User Detail + ช่วย-user tools (+ แก้ API) | ปลดล็อกเป้า 1,000 users (P0-3) |
| **D. MCP Server + Developer surface** | token model, MCP server, หน้า Developers | requirement ใหม่ ต่อยอดหลังฐานนิ่ง |

---

## 4. Wireframe / Mockup

ดูไฟล์ mockup แบบ interactive (ส่งแยกเป็น artifact) — ครอบคลุมจอสำคัญ: Dashboard ใหม่ (1-CTA), First-run onboarding, Wizard step (ไทย), Tools (ภาษาคน), Admin Operations Console + User Detail, และหน้า Developers/MCP

---

## 5. Implementation status (อัปเดตทุกครั้งที่เฟสเสร็จ)

User สั่งลุยเป็นเฟส ๆ แบบ auto ไม่ตามลำดับที่เสนอไว้ใน §3 เป๊ะ (ข้าม B ไปทำ C/D ก่อน) — ตารางนี้คือของจริงที่ทำไปแล้ว ไม่ใช่แผนที่เสนอ

| เฟส | สถานะ | รายละเอียด |
|---|---|---|
| **A. Design System + Onboarding + Dashboard** | 🟡 บางส่วน | Dashboard consolidation เสร็จ (1 primary CTA, true-first-time empty state ชี้ไปเครื่องมือเดี่ยว 3 ตัว, gradient banner/StrategicToolsWidget โชว์เฉพาะ returning user) — commit `32a06c3`. **ยังไม่ทำ**: design token ชุดเต็ม (neutral scale, semantic colors, dark mode) ใน `tailwind.config.js`/`app.css` |
| **B. ภาษา + Landing + Wizard + Tools** | ✅ เสร็จ (ส่วนหลัก) | Wizard: step names เป็นไทย+เลข+ไอคอน (English เป็น subtitle), helper text อธิบาย "ปัญหาลูกค้า (Pain Point)", mobile ใช้ dot-progress แทน tab แถวเดิม (ไม่ล้นจอ), แสดงประมาณการเครดิตก่อนกด Generate ("ใช้ประมาณ ~N เครดิต"). Tools page: เขียนใหม่เป็น "ช่วยคุณ___" ภาษาคนทั้ง 10 ตัว จัดหมวด 3 กลุ่ม (คิดกลยุทธ์/เขียนคอนเทนต์/ขาย), เอา badge NEW ออกจาก 7 ใน 8 ตัว (เหลือเฉพาะ Presentation Builder ที่ใหม่จริง). Mobile: เพิ่ม bottom tab bar (`$lib/MobileNav.svelte`, ใน root layout) — หน้าแรก/เครื่องมือ/เครดิต/โปรไฟล์ — และย้าย เติมเงิน/โปรไฟล์/บันทึก/Developers/logout ออกจาก dashboard header บนมือถือ (ล้นจอเดิม) ไปไว้ใน profile page การ์ด "บัญชี" ใหม่ (ซึ่งก่อนหน้านี้ไม่มีปุ่ม logout เลย). **ยังไม่ทำ**: landing page เพิ่มเติม (โครงสร้างหลักแก้ไปแล้วตอน rebrand), icon set เดียวแทน emoji ทั้งระบบ (P2-1) |
| **C. Admin Operations Console** | ✅ เสร็จ | Backend: search/role/verified filter + real pagination บน `GET /api/admin/users` (`apps/api/src/adminRoutes.ts`), `GET /api/admin/users/:id` (profile+credit history+projects+activity+admin_actions), resend-verification/send-password-reset/notes actions, funnel+signups-by-day บน `/api/admin/stats`. Frontend: `apps/web/src/routes/admin/+page.svelte` สร้างใหม่ทั้งหน้า (Overview/Users/Emails tabs + User Detail modal). Code-reviewer เจอ 2 bug จริง (credit-deduction failure ที่รายงาน false success, missing-existence-check ทำ 500 แทน 404) — แก้และ verify แล้วทั้งคู่ |
| **D. MCP Server + Developer surface** | ✅ เสร็จ | `apps/api/src/mcpRoutes.ts` ใหม่ — JSON-RPC `/mcp` endpoint (initialize/tools-list/tools-call), token model ใหม่ (`mcp_tokens` table, migration `008-mcp-tokens.sql`, hash เก็บไม่เก็บ raw token), 6 tools (`list_projects`/`create_project`/`generate_step`/`run_tool`/`get_export`/`check_credits`) dispatch ผ่าน REST handler เดิมด้วย internal short-lived session แทนการเขียน credit/AI logic ซ้ำ. หน้า `/developers` ใหม่ (สร้าง/revoke token, copy `claude mcp add` command + JSON config). Verified live ทั้ง 6 tools ผ่าน wrangler dev จริง รวมถึง run_tool ที่เรียก MiniMax จริงและหักเครดิตจริง |
| **D2. Developer docs page** | ✅ เสร็จ | หน้า `/developers/docs` ใหม่ — quickstart 3 ขั้น, ตาราง parameter + ตัวอย่าง arguments/response ของทั้ง 6 tools (อ้างอิงจาก schema จริงใน `mcpRoutes.ts`), ตาราง error ที่เจอบ่อย, FAQ. ลิงก์จากหน้า `/developers` หลัก. แก้ mobile overflow bug ที่เจอเอง (ตาราง parameter/error ล้นจอ 375px) เปลี่ยนเป็น list ที่ reflow ได้ก่อน ship |

| **E. Design tokens (ส่วนที่เหลือของ A, P2-5)** | 🟡 บางส่วน | `tailwind.config.js`: เติม neutral scale `dark` ให้ครบ 50-950 (เดิมกระโดด 200→900), เพิ่ม semantic color `success`/`warning`/`danger`/`info`, เปิด `darkMode: 'class'`. Dark mode toggle ใช้งานได้จริง (`$lib/theme.ts` + toggle ในหน้า Profile) — **แปลงเต็มแค่หน้า Profile** (หน้าที่ toggle อยู่) เป็น proof ว่าระบบทำงานจริง ส่วนหน้าอื่นทั้งหมด (~40 ไฟล์) ยังเป็น light-mode-only โดยตั้งใจ — แปลงทีละไฟล์เป็นงานต่อเนื่องที่ยังไม่ทำ (สาเหตุ: แต่ละหน้า hardcode Tailwind class ตรง ๆ ไม่มี shared surface component ให้แก้จุดเดียว) |

| **F. แก้ P1-4 (create-project form ล้นจอ)** | ✅ เสร็จ | ปุ่ม "+ สร้างแผนใหม่" เดิม toggle ฟอร์ม inline ที่ render หลัง gradient banner + StrategicToolsWidget + kind filters สำหรับ returning user — กดแล้ว "ไม่มีอะไรเกิดขึ้น" เพราะฟอร์มอยู่นอกจอ ต้อง scroll เองถึงจะเห็น. แก้ด้วย `openCreate()` — set `showCreate=true` → `tick()` → `scrollIntoView({behavior:'smooth'})` + `.focus()` ที่ช่องชื่อธุรกิจ ทั้ง 3 จุดที่เปิดฟอร์ม (hero button, ทั้ง 2 empty state) |

| **G. Rebrand cleanup (เศษ "AI" ที่รอดจากรอบ rebrand)** | ✅ เสร็จ | ระหว่างหา P2-4 (sticky header bug — ค้นทั้ง repo ไม่เจอ CSS pattern ที่ทำให้เกิดจริง อาจถูกแก้ไปแล้วโดยบังเอิญตอน refactor รอบก่อน ๆ) เจอเศษคำว่า "AI" ที่รอดจากรอบ rebrand แทน: helper text ใน wizard (`StepAssets.svelte`), landing page nav tooltip, หัวข้อเครดิตในหน้า Profile, preset อุตสาหกรรม 2 จุด (`businessTypePresets.ts`) — แก้เป็น "ระบบอัจฉริยะ" ทั้งหมด. เจอจุดสำคัญกว่านั้นคือ **อีเมลยืนยันตัวตน** (ส่งให้ user ใหม่ทุกคน) มี badge เขียนผิดว่า "MARKETING AI OS" (ชื่อผลิตภัณฑ์ที่ไม่เคยใช้จริง) — แก้เป็น "Business Smart OS" verify กับอีเมลจริงที่ส่งออกแล้ว. code-reviewer เจอเพิ่มอีก 1 จุดจริงที่ grep เดิมพลาด: ไฟล์ .pptx ที่ export ออกจาก Presentation Builder มี metadata `subject` เขียนว่า "AI-Generated Presentation" (เห็นได้จาก File→Properties ใน PowerPoint) — แก้เป็น "Smart Engine-Generated Presentation" พร้อม back-test จริงด้วยการ unzip ไฟล์ pptx ที่ generate ออกมาตรวจ XML |

| **H. Dark mode rollout ต่อจาก E (dashboard)** | ✅ เสร็จ | ขยาย dark mode จากหน้า Profile (เฟส E) มาที่ `apps/web/src/routes/dashboard/+page.svelte` ทั้งหน้า (header, hero, error/action banner, ฟอร์มสร้างโปรเจกต์+kind picker, kind-filter pills, project card grid + status badge, actions dropdown menu, archive section) ตาม color-mapping convention เดิมจากเฟส E. ระหว่าง live-test เจอ 2 bug จริงเพิ่มเอง: (1) `StrategicToolsWidget.svelte` (sub-component ที่ฝังในหน้า dashboard) ไม่เคยถูกแปลง dark mode มาก่อนเลย เห็นเป็นการ์ดสีขาว/gradient สว่างจ้าตัดกับพื้นหลังมืด — แปลงแล้วครบ; (2) `app.html`'s `<body>` tag hardcode `class="bg-white text-dark-900"` (Tailwind utility class ที่ชนะ `@layer base` เสมอ) ทำให้ scroll overscroll/bounce ใน dark mode เห็นพื้นขาววาบ — แก้เป็น `dark:bg-dark-950 dark:text-dark-50` โดยตรงใน body tag, verify แล้วว่า computed body background เปลี่ยนจาก `rgb(255,255,255)` เป็น `rgb(5,5,5)` จริง. code-reviewer เจอเพิ่มอีก 3 bug จริง: (a) `dark:bg-primary-950/50` ×3 จุด (credit badge, selected kind-pill, empty-state icon) ใช้ shade `950` ที่ `tailwind.config.js` ไม่มีจริง (custom `primary` scale มีแค่ 50-900) — Tailwind JIT ทิ้ง class เงียบ ๆ ทำให้พื้นหลัง+ตัวหนังสือเป็นสีขาวซ้อนขาว (มองไม่เห็นเลย ยืนยันด้วย `getComputedStyle` จริง) — แก้เป็น `dark:bg-primary-900/40`; (b) ลิงก์ "💰 เติมเงิน" ในเมนู ไม่มี `dark:hover:text-dark-50` เหมือน sibling อื่น ทำให้ hover ใน dark mode ได้ตัวหนังสือเกือบดำสนิทบนพื้นมืด — เพิ่มให้ครบ; (c) gradient การ์ด StrategicToolsWidget มี 2 ใน 3 stop เป็นสีเดียวกัน (`dark:via-dark-900 dark:to-dark-900`) ทำให้ gradient จริง ๆ ใช้ได้แค่ครึ่งการ์ด — แก้ `to` เป็น `dark:to-dark-950`. Kind-badge สีไดนามิก (`bg-{kindMeta.color}-50`) ยังคงตั้งใจไม่แปลง เหมือนเฟส E. Verify ครบ: typecheck (54-error baseline ไม่เปลี่ยน) + build ผ่าน + live-verify ทั้ง light/dark ผ่านเบราว์เซอร์จริง (ทั้งก่อนและหลัง code-reviewer แก้). หน้า login/register ยังไม่แปลง (คิวถัดไปถ้าทำต่อ) |

| **I. Dark mode: login + register** | ✅ เสร็จ | ขยาย dark mode ต่อจาก Phase H (dashboard) มาที่หน้า `login`/`register` (สองหน้านี้ไม่เคยแปลง dark mode มาก่อนเลย) — ตัว auth card (`bg-white`→`dark:bg-dark-800`), พื้นหลัง gradient หน้า, ช่อง input (อาศัย `color-scheme: dark` ที่ตั้งไว้แล้วจาก `app.css` ทำให้ input เปลี่ยนสีพื้นหลัง/ตัวอักษรเองโดยไม่ต้องใส่ class เพิ่ม — ยืนยันแล้วว่าไม่ใช่ white-on-white), ปุ่ม toggle แสดง/ซ่อนรหัสผ่าน, error banner. **เพิ่ม convention ใหม่ที่ยังไม่เคยใช้มาก่อน**: ลิงก์สี `text-primary-600` (เช่น "ลืมรหัสผ่าน?", "สมัครสมาชิก") ได้ `dark:text-primary-400` เพิ่ม — เป็นการตั้งใจไม่ตามรอย pattern เดิมที่หน้า Profile/Dashboard ปล่อย `text-primary-600` ไว้แบบไม่มี dark variant (contrast จริงแค่ ~2.8:1 ต่ำกว่ามาตรฐาน WCAG AA แต่ยังไม่ได้แก้ที่หน้าเก่า — เป็นช่องโหว่ที่รู้อยู่แล้ว ไม่ใช่งานของรอบนี้) เพราะหน้านี้เป็น dark-mode surface ใหม่ทั้งหมด ไม่อยากให้ของใหม่มีปัญหาเดิมซ้ำ — code-reviewer คำนวณ contrast จริงยืนยันว่า primary-400 บนพื้น dark-800 ได้ ~5.75:1 ผ่าน AA. code-reviewer ตรวจสอบครบทุกจุดที่เคยเป็นบั๊กในเฟสก่อน (shade ที่ไม่มีจริงใน config, gradient แบนราบ, input ขาวซ้อนขาว) — **ไม่เจอบั๊กเลย ครั้งแรกที่ผ่านโดยไม่ต้องแก้อะไร**. Verify ครบ: typecheck (54-error baseline เดิม) + build ผ่าน + live-test จริงทั้ง light/dark รวมถึง trigger error banner จริง (ใส่รหัสผ่านผิด) และกดปุ่ม toggle รหัสผ่านจริง |

| **J. Dark mode: project wizard (7 ขั้นตอน)** | ✅ เสร็จ | ขยาย dark mode มาที่หน้าที่ user ใช้เวลาอยู่นานที่สุดในระบบทั้งหมด — `routes/projects/[id]/+page.svelte` (wizard shell 564 บรรทัด: header, step-tabs/step-dots, ฟอร์ม 7 ขั้นตอน, ปุ่ม generate, export panel), `lib/StepAssets.svelte` (296 บรรทัด: accordion โน้ต/ไฟล์/ลิงก์ต่อ step), และ `lib/OutputRenderer.svelte` (480 บรรทัด: การ์ดแสดงผลลัพธ์ที่ระบบอัจฉริยะ generate ให้ — นี่คือเนื้อหาที่ user จ่ายเครดิตเพื่อดู ความชัดเจนสำคัญที่สุดในทั้งระบบ). ทั้ง 3 ไฟล์ไม่เคยแปลง dark mode มาก่อนเลย ตาม convention เดิมจากเฟส E/H/I. **เจอ+แก้บั๊กจริงเองระหว่างทำ** (ก่อนส่งรีวิว): `dark:from-primary-950/40` ใช้ shade ที่ไม่มีจริงใน config (ซ้ำกับบั๊กเดิมที่เจอใน Phase H) ทำให้การ์ด gradient 3 จุดใน OutputRenderer ยังโชว์สีอ่อนแบบ light mode — จับได้จาก `getComputedStyle` จริงที่เห็น backgroundImage เป็น primary-50 ทั้งที่ theme เป็น dark — แก้เป็น `dark:from-primary-900/40`; และบั๊กจาก bulk regex script เอง (bare selector ไปแมตช์ซ้ำกับ substring ใน hover:-prefixed class) ทำให้เกิด class ซ้ำ/เสีย opacity suffix หลายจุด (step-tab hover, nav-link hover ×4, StepAssets accordion toggle hover) — แก้หมดแล้วก่อนส่งรีวิว. **code-reviewer เจอเพิ่มอีก 1 จุดที่หลุดรอดสายตา**: ปุ่ม "Copy JSON" ได้ `dark:text-primary-400` แบบไม่มี `hover:` prefix ทำให้ตอน dark mode ปุ่มเป็นสี primary-400 ตลอดเวลาแทนที่จะเป็นแค่ตอน hover (ปกติควรเป็น `text-dark-900/60`-ish ตอนพัก) — แก้เป็น `dark:hover:text-primary-400` verify แล้วว่า rest-state กลับมาเป็น dark-100/60 และ hover ถูกต้อง. Verify ครบ: typecheck (54-error baseline เดิม) + build ผ่าน + สร้างโปรเจกต์ทดสอบจริง กรอกข้อมูล generate step 1 จริง (เสียเครดิตจริงไม่กี่หน่วย) เช็ค gradient card/badge/note card ด้วย getComputedStyle ทั้ง 2 ธีมจริง แล้วลบโปรเจกต์ทดสอบทิ้ง |

| **K. Dark mode: tools index, reset-password, verify-email** | ✅ เสร็จ | หน้าเล็ก 3 หน้าที่ไม่เคยแปลง dark mode: `/tools` (grid การ์ดเครื่องมือ 11 ตัว + badge NEW + CTA banner), `/reset-password` (flow email→OTP→done 3 ขั้น), `/verify-email` (status card). ตาม convention เดิม. **code-reviewer ตรวจแล้วไม่เจอบั๊กเลย** (ครั้งที่ 2 ที่ผ่านโดยไม่ต้องแก้อะไร) — เช็คเฉพาะเจาะจง 2 บั๊กที่เคยเจอในเฟสก่อน (primary-950 ที่ไม่มีจริง, hover-class ซ้ำ/opacity เสีย) ไม่พบทั้งคู่ ยืนยันด้วย getComputedStyle จริงรวมถึง hover state จริง (ไม่ใช่แค่ screenshot) ทั้ง 2 ธีม. Verify ครบ: typecheck (54-error baseline เดิม) + build ผ่าน |

**ยังไม่ทำในทุกเฟส**: component library (button/input/modal/toast แบบ reusable), icon set เดียวแทน emoji ทั้งระบบ, Turnstile เปิดใช้งานจริง (ปิดไว้ตั้งใจ — ดู STATUS.md), dark mode ครบทุกหน้า (ดูแผนย่อยด้านล่าง §5.1), primary-600 link contrast gap บนหน้า Profile/Dashboard เก่า (รู้แล้วว่ามี แต่ยังไม่แก้ย้อนหลัง), sticky header bug (P2-4 — หาไม่เจอ อาจไม่มีอยู่แล้ว), 22 จุดใน error message ภายใน (`'AI call failed'` ฯลฯ) ที่เห็นได้เฉพาะใน admin panel เท่านั้น — ทิ้งไว้ตั้งใจ (ไม่ใช่หน้าที่ corporate network filter จะ block)

### 5.1 แผนย่อย: dark mode rollout ที่เหลือ (เฟส L เป็นต้นไป — proposal, ยังไม่เริ่ม)

หลังเฟส K แปลงไปแล้ว 9 หน้า + 4 sub-component (Profile, Dashboard+Widget, Login, Register, Wizard+StepAssets+OutputRenderer, Tools index, Reset-password, Verify-email) เหลืออีก ~30 หน้า/ไฟล์ รวมกันใหญ่กว่าทุกเฟสที่ทำมาทั้งหมดรวมกัน (เฟส J ซึ่งใหญ่ที่สุดที่เคยทำ ~1,340 บรรทัด ใช้ code-reviewer รอบเดียวกินเวลา ~22 นาที/~243k tokens) — **ห้ามทำทีเดียวหมดเป็น diff เดียว** ต้องแบ่งเป็นเฟสย่อยขนาดใกล้เคียงเฟสที่ผ่านมา (~1,200-1,700 บรรทัด/เฟส) ตามลำดับนี้:

| เฟส (proposal) | ไฟล์ | บรรทัดรวม | หมายเหตุ |
|---|---|---|---|
| **L. Shared tool components** | `lib/ToolLayout.svelte` (92), `lib/BusinessContextFields.svelte` (187), `lib/ToolChainHint.svelte` (56), `lib/CanvasPdfButton.svelte` (44) | 379 | **ทำก่อนเป็นอันดับแรก** — เช็คแล้วว่า 10 หน้าเครื่องมือ AI ทั้งหมด import `ToolLayout` ร่วมกัน (header/shell), 9/10 ใช้ `BusinessContextFields` (ฟอร์มกรอกข้อมูลธุรกิจร่วม), 6/10 ใช้ `ToolChainHint`, 5/10 ใช้ `CanvasPdfButton` — แปลง 4 ไฟล์นี้ครั้งเดียว ได้ผลกับทุกหน้าเครื่องมือทันที (high-leverage เหมือน StrategicToolsWidget ตอน Phase H) |
| **M. Tools batch 1 (เล็กสุด)** | `tools/pain-generator` (448), `tools/brand-voice` (465), `tools/hook-library` (488) | 1,401 | ✅ **Stage 1 (UI/dark mode) เสร็จแล้ว** — แปลง 4 shared component ก่อน (`ToolLayout`, `BusinessContextFields`, `ToolChainHint`, `CanvasPdfButton` — leverage สูงเพราะใช้ร่วมกันทั้ง 10 หน้าเครื่องมือ) แล้วค่อยแปลง 3 หน้านี้ ตาม convention เดิม. code-reviewer เจอ+แก้บั๊กจริง 1 จุด: input/textarea 18 จุดทั่วทั้ง diff (ชื่อธุรกิจ, ที่ตั้ง, ราคา, notes ฯลฯ) ไม่มี `dark:bg-dark-800` explicit ทำให้ browser ใช้ `color-scheme:dark` default (สีเทาแบนๆ `rgb(59,59,59)`) แทนสี dark-800 ที่การ์ด/select ข้างๆ ใช้ — ไม่ใช่ปัญหา contrast (ตัวหนังสือยังอ่านออก) แต่สีไม่ตรงกับ UI รอบข้าง แก้แล้วครบ. Verify ครบ: typecheck (54-error baseline เดิม) + build ผ่าน + generate จริง 2 ครั้ง (pain-generator, hook-library) เช็คสี severity card/category badge/quick-wins/moonshots ด้วย getComputedStyle จริงทั้ง 2 ธีม + WCAG contrast คำนวณจริงทุกคู่สี (7:1-12:1 ผ่าน AA หมด). ✅ **Stage 2 (แก้ prompt 3 ตัวนี้ใน prompts.ts) เสร็จแล้ว** — เพิ่ม field แบบ additive ล้วน (ไม่แตะโครงเดิม): pain-generator ได้ `priority_pick`+`validation_note`, brand-voice ได้ `voice_attributes` (means/does-not-mean)+`self_check_list`, hook-library ได้ `recommended_pick`+guardrail กันแต่งตัวเลข/รายละเอียดปฏิบัติการ (ระยะทาง/เวลาเปิด-ปิด/ขั้นตอน) มั่ว. เพิ่ม UI รองรับ field ใหม่ทุกตัวในหน้าเครื่องมือเอง (ไม่ใช่ OutputRenderer ของ wizard). ทดสอบ generate จริง 6 ครั้ง (2 ครั้ง/เครื่องมือ) ยืนยันเนื้อหาตรงประเด็นจริง ไม่ generic. code-reviewer เจอ+แก้บั๊กจริง 1 จุด: guardrail กันแต่งตัวเลขเดิมคุมแค่ %/ยอดขาย แต่ AI ยังแต่งรายละเอียดปฏิบัติการ (ระยะทาง 800 กม., เวลาเปิด-ปิดเจาะจง, ระยะเวลาคั่ว) ที่ไม่มีหลักฐาน — ขยาย guardrail ให้ครอบคลุมและ re-test ยืนยันว่า AI รายงานความสอดคล้องเองใน `reasoning` field ("ไม่แต่งตัวเลข...ใช้มุมเน้นกระบวนการแทน"). ยืนยันด้วยว่า `Number(recommended_pick.index) === i` coerce ทั้ง string/number type ที่ AI ตอบมาถูกต้อง (เจอ AI ตอบ index เป็น string จริง ในการทดสอบ). เช็ค token budget จริง: completion_tokens ใช้ 41-56% ของ reserve ทั้ง 3 เครื่องมือ ไม่เสี่ยง under-reserve. Verify ครบ: typecheck ทั้ง web (54-error baseline)+api (41-error baseline) + build ผ่าน + vitest 38/38 ผ่าน + generate จริงครบทั้งก่อน-หลังแก้บั๊ก ทั้ง 2 ธีม |
| **N. Tools batch 2** | `tools/persona-builder` (484), `tools/objection-handler` (509) | 993 | ✅ เสร็จแล้ว (dark mode + prompt เพิ่ม field แบบ additive). **presentation-builder แยกออกเป็นเฟสของตัวเอง** (ดูแถวถัดไป) — ระหว่างเริ่มพบว่า shell 519 บรรทัดที่วางแผนไว้ไม่ใช่ทั้งหมด จริงๆ delegate ไปยัง 9 step sub-component (`steps/Step1Brief.svelte` ถึง `Step9Export.svelte`) รวมอีก ~1,210 บรรทัดที่ไม่เคยนับไว้ในแผน — ขนาดจริงใหญ่กว่าที่ประเมินไว้เกือบ 2 เท่า เลยแยกเป็นเฟสแยกแทนที่จะยัดรวม. **prompt improvements**: persona-builder ได้ `day_in_life`+`sample_quotes` (narrative ให้ persona เป็น "คน" ไม่ใช่ list attribute) + `buying_behavior` (research_style/decision_speed/objections) + cap จำนวน persona 1-3; objection-handler ได้ `explore_question` (คำถามสำรวจก่อนตอบ ตาม LAER's Explore step ที่ prompt เดิมข้ามไป) + `second_response` (ถ้าลูกค้าโต้กลับอีกรอบ + แนวทางถอยอย่างมีศักดิ์ศรี) + guardrail เรื่องจุดอ่อนสินค้าจริง. **code-reviewer เจอ+แก้บั๊กจริง 1 จุด**: ปุ่ม "← สร้างใหม่" ใน persona-builder ขาด `dark:hover:text-primary-400` (มี contrast แค่ 2.83:1 ตอน hover ใน dark mode) ต่างจาก sibling ใน objection-handler ที่มีอยู่แล้ว — แก้ให้ตรงกัน. **เจอ+แก้ risk จริงอีกจุด**: objection-handler's token reserve (`toolMaxTokens`) เดิม 16000 ถูกใช้ไปถึง 88% (14104 tokens) จาก field ใหม่ที่เพิ่มเข้าไป เสี่ยง JSON ถูกตัดกลางถ้า AI ตอบ 8 objections เต็มๆ — bump เป็น 20000. Verify ครบ: typecheck (54-error web / 41-error api baseline เดิม) + build + vitest 38/38 + generate จริงหลายครั้งยืนยัน persona cap ที่ 1-3 จริง, ครอบคลุมครบ 7 objection categories จริง |
| **N+. Presentation Builder (แยกเฟส)** | `tools/presentation-builder/+page.svelte` (519, shell) + `steps/Step1Brief.svelte` ถึง `Step9Export.svelte` (9 ไฟล์ ~1,210 บรรทัด) | ~1,729 | ✅ เสร็จแล้ว — dark mode แปลงครบทั้ง shell+9 step. **เจอ+แก้บั๊กจริงเอง 2 จุด**: (1) gradient card "🚀 สร้าง Presentation ใหม่" (`from-purple-50 to-blue-50`) แปลงแค่ border ไม่ได้แปลง gradient stop เอง (`from-`/`to-` เป็นคนละ prefix กับ `bg-`) ทำให้การ์ดยังโชว์ขาวเต็มใน dark mode — จับได้จาก screenshot จริง ไม่ใช่แค่ grep — แก้เป็น `dark:from-purple-950/40 dark:to-blue-950/40`; (2) **บั๊กจริงที่ไม่เกี่ยวกับ dark mode แต่เจอระหว่างทดสอบ**: `POST /api/presentation/projects` และ `GET /api/presentation/projects` อ้างอิง column `framework_variant` บน table `presentation_projects` — แต่ column นี้มีอยู่จริงแค่บน `presentation_steps` เท่านั้น (ดู migration `003-presentation-tool.sql`) ทำให้ **เครื่องมือนี้สร้าง presentation project ใหม่ไม่ได้เลยตั้งแต่แรก** (ยืนยันด้วย D1 error จริง) — แก้โดยลบ reference ที่ผิดออกจาก query. **code-reviewer ตรวจ E2E ผ่านทุก step (1-8) จริง ด้วย getComputedStyle ทั้ง 2 ธีม — ไม่เจอบั๊กเพิ่ม, ยืนยัน framework_variant fix ใช้งานได้จริงด้วยการสร้างโปรเจกต์จริง**. **เจอบั๊กจริงเพิ่มอีก 2 จุด (นอก scope ไม่แก้ในรอบนี้)**: (a) `GET /api/presentation/projects/:id` ไม่ select `input_json` เลย ทำให้ step ที่เก็บข้อมูลใน `input_json` เท่านั้น (เช่น Step 4 source content) หายไปจาก UI ทันทีที่เปลี่ยน step แม้จะ save ลง D1 ถูกต้องแล้ว — บล็อกทั้ง wizard flow ตั้งแต่ step 4→5; (b) `Step9Export.svelte` ใช้ dynamic class interpolation (`hover:border-{fmt.color}-500`) ที่ Tailwind scanner ตรวจไม่เจอ ทำให้ hover state น่าจะไม่ทำงานเลยทั้ง 2 ธีม — ทั้งสองจุดนี้ preexisting ไม่เกี่ยวกับ dark mode/framework_variant fix. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม, ยืนยันด้วย git stash) + build + vitest 38/38 |
| **O. Tools batch 3** | `tools/competitor-analysis` (655), `tools/value-proposition-canvas` (702) | 1,357 | ✅ เสร็จแล้ว (dark mode + prompt เพิ่ม field แบบ additive). ทั้งคู่ใช้ palette สีเยอะที่สุดในกลุ่มที่ทำมา (competitor-analysis 9 สี, VPC 12 สี) รวม gradient 13 จุด (4+9) ที่ต้องแปลง `from-`/`to-` stop เองแยกจาก `bg-`. **prompt improvements**: competitor-analysis ได้ `response` ต่อคู่แข่งแต่ละราย (แยกจาก strategy รวม) + guardrail กัน threat_level เป็น "high" ทุกเจ้า; VPC ได้ `messaging_hierarchy` (primary message + supporting + proof points พร้อมใช้เขียนโฆษณา) + `application_guide` (headline โฆษณา/landing copy/sales talking points พร้อมใช้จริง) + instruction เลือก segment เดียวถ้า input มีหลาย segment ปน. **code-reviewer เจอ+แก้บั๊กจริง 1 จุด**: ระหว่างแปลง dark mode เผลอทำ `text-orange-700/70`/`text-teal-700/70` (opacity suffix) หลุดหายจาก light-mode class กลายเป็น `text-orange-700 dark:text-orange-400/70` — ทำให้ light mode แสดงข้อความทึบเต็มแทนที่จะจางลง 70% ตามตั้งใจเดิม — แก้กลับเป็น `text-orange-700/70 dark:text-orange-400/70` และ verify ด้วย getComputedStyle จริงทั้ง 2 ธีม. **bump token budget VPC**: 10000→14000 หลัง field ใหม่ดันการใช้งานจริงขึ้นไปถึง 66.9-78% ของ reserve เดิม (competitor-analysis ทดสอบแล้วอยู่ที่ 40-55% ปลอดภัยดี ไม่ต้อง bump). Verify ครบ: typecheck (54-error web/41-error api baseline เดิม, ยืนยันด้วย git stash) + build + vitest 38/38 + generate จริงหลายครั้งยืนยัน threat_level มี variance จริง (ไม่ high ทุกเจ้า) และเนื้อหา messaging/application เฉพาะธุรกิจจริง ไม่ generic |
| **P. Tools batch 4** | `tools/jtbd-generator` (713), `tools/million-dollar-offer` (739) | 1,452 | ✅ เสร็จแล้ว (dark mode + prompt เพิ่ม field แบบ additive). ทั้งคู่มี palette สีเยอะ (jtbd-generator 8 สี + gradient 8 จุด รวม 3 คู่ `from-indigo-50 to-purple-50` ซ้ำกันในบริบทต่างกัน ต้อง disambiguate ทีละจุด; million-dollar-offer 9 สี + gradient 12 จุด). **prompt improvements (audit-first ตาม PROMPT_IMPROVEMENT_PLAN.md §P)**: jtbd-generator เจอ 2 ช่องว่างจริงเทียบ Christensen/Moesta/Ulwick — ขาด Ulwick Job Map (8 ขั้นตอน, คนละ framework จาก Moesta Timeline 5 stages ที่มีอยู่แล้ว) และขาด Hiring/Firing criteria (Moesta's switch interview) ทั้งที่รับ current_solutions/known_objections เป็น input อยู่แล้วแต่ไม่เคยวิเคราะห์ออกมา — เพิ่ม field `job_map`+`hiring_firing_criteria`; million-dollar-offer เจอ 2 จุดเทียบ Hormozi Value Equation — ratio criteria ที่บังคับจริง (`≥3:1`) หลวมกว่าที่ system prompt เองอ้าง (5-10x) และไม่มี guarantee **stack** จริง (Hormozi เน้นซ้อน guarantee คนละมุม) — แก้ ratio เป็น 5:1-10:1 + เพิ่ม field `secondary_guarantee` (มี `applicable` flag กัน AI ยัด guarantee ปลอม). **เจอ+แก้บั๊กจริงเอง 2 จุดระหว่างแปลง dark mode**: static color-map functions (`intensityColor`/`scoreColor` ใน jtbd-generator) มี fallback branch ขาด `dark:` variant (ซ้ำ pattern เดิมที่เจอในเฟส O) — แก้ก่อนส่งรีวิว; TypeScript index-signature error จาก object literal lookup (`jobMapLabels[step.step]`) — cast เป็น `Record<string,string>` แก้แล้ว. **bump token budget jtbd-generator**: 10000→13000 หลังทดสอบจริง 2 ครั้งเจอ completion_tokens ใช้ 71-80% ของ reserve เดิม (million-dollar-offer ทดสอบแล้วอยู่ที่ 62.6% ปลอดภัย ไม่ต้อง bump). Browser preview flaky ระหว่างทดสอบ million-dollar-offer (blank/stuck pane) — เปลี่ยนไปเทสตรงผ่าน API call จริง (curl + local D1 query) แทน ยืนยันผลเหมือนกัน. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม, ยืนยันด้วย git stash) + build + vitest 38/38 + generate จริงหลายครั้งยืนยัน job_map ครบ 8 steps เจาะจงธุรกิจจริง, hiring/firing criteria อ้างอิงข้อมูลที่ user กรอกจริง, price ratio อยู่ในช่วง 5:1-10:1 จริง, secondary_guarantee คนละมุมกับตัวหลักจริง |
| **Q. Tools batch 5** | `tools/business-model-canvas` (811) | 811 | ✅ เสร็จแล้ว (dark mode + prompt เพิ่ม field แบบ additive). ไฟล์เดี่ยวที่ใหญ่ที่สุดในกลุ่มเครื่องมือ ทำเดี่ยวเหมือน batch เล็ก — palette สีเยอะที่สุดในทั้งรอบ (14 สี) รวม gradient 9 จุด (8 จุดต้องแปลง `dark:from-`/`dark:to-`, 1 จุดเป็น badge สีทึบ `from-indigo-500 to-purple-600` ที่ตั้งใจไม่แปลงตาม convention leverColor เดิม). **prompt improvements (audit-first ตาม PROMPT_IMPROVEMENT_PLAN.md §Q)**: เจอช่องว่างใหญ่เทียบ Osterwalder BMC — 9 blocks เดิมเป็น list แยกกัน ไม่เชื่อมโยงกันเป็นระบบ (audit instruction เองระบุให้เช็คจุดนี้) — เพิ่ม `for_segment` ใน revenue_streams (เชื่อม revenue↔segment) + `supports_vp` ใน key_resources/key_activities/key_partnerships (เชื่อม resource↔value proposition) + `model_coherence` (narrative เชื่อม segment→VP→revenue→resource เป็น logic chain เดียว) พร้อม instruction บังคับอ้างชื่อจริงไม่ hallucinate. **เจอ+แก้บั๊กจริงเอง 2 จุดระหว่างแปลง dark mode** (ก่อนส่งรีวิว): (1) opacity-suffix corruption ซ้ำ pattern เดิมจากเฟส O — `text-blue-700/70`/`text-purple-700/70`/`text-red-700/70` หลุด `/70` จาก light mode; (2) **บั๊กใหม่จาก regex script เอง**: กฎทั่วไป (เช่น `text-purple-700`) ไป match ซ้ำกับจุดที่มี opacity suffix ที่ถูกแปลงไปแล้ว ทำให้เกิด `dark:text-purple-400/70 dark:text-purple-400/70` (ซ้ำ 2 ครั้ง) — แก้ทั้งหมดด้วย string replace ตรงจุด ก่อนส่งให้ code-reviewer ตรวจซ้ำ. **bump token budget**: 12000→15000 หลังทดสอบจริง 4 ครั้งเจอ completion_tokens ใช้ 54-71% ของ reserve เดิม/ใหม่. ทดสอบ generate จริงยืนยัน: `for_segment`/`supports_vp` ทุกจุดอ้างอิงชื่อ segment/VP จริงที่มีอยู่ในผลลัพธ์เดียวกัน ไม่ hallucinate. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |
| **R. Saved tools page** | `tools/saved/+page.svelte` | 1,765 | ✅ เสร็จแล้ว (dark mode เท่านั้น — หน้านี้ไม่เรียก AI prompt เอง แค่แสดงผลลัพธ์ที่บันทึกไว้แล้ว จึงไม่มี Stage 2). หน้าใหญ่ที่สุดในทั้งระบบรองจาก wizard bundle ทำเป็นรอบเดียว (ไม่ต้องแบ่ง 2 รอบตามที่ประเมินไว้) — เป็น per-tool-type switch ที่ render ผลลัพธ์ของทั้ง 10 เครื่องมือแบบ inline (คล้าย OutputRenderer.svelte ของ wizard) จึงใช้สีเกือบทุกสีที่มีในระบบ (14 สี, gradient 10 จุด — 7 จุดแปลง dark:from-/to- + 3 จุดเป็น badge สีทึบที่ตั้งใจไม่แปลง). **สำคัญ**: ไฟล์นี้มีฟังก์ชัน `buildPrintableHTML()` ที่ generate เอกสาร HTML แยกต่างหาก (เปิด popup ใหม่สำหรับ print/PDF) — มี `<style>` เป็น hex color ของตัวเอง ไม่ใช่ Tailwind class และตั้งใจไม่แปลง dark mode เพราะเป็นเอกสารสำหรับพิมพ์กระดาษ ต้องคงพื้นขาว-ตัวหนังสือเข้มเสมอ (เหมือน canvasTemplates.ts ที่ไม่ถูกแตะในเฟสก่อนๆ). **เจอ+แก้บั๊กจริงเอง 2 จุดระหว่างแปลง** (ก่อนส่งรีวิว): (1) opacity-suffix corruption ซ้ำ pattern เดิม — `text-amber-700/70`/`text-orange-700/70`/`text-pink-700/70` หลุด `/70` จาก light mode; (2) **บั๊กจาก regex script เอง (rule ซ้ำในลิสต์)**: กฎเดียวกันถูกเขียนซ้ำในสคริปต์ ทำให้ apply 2 รอบ เกิด `dark:text-amber-400/70 dark:text-amber-400/70` (ซ้ำ 2 ครั้ง) — แก้หมดแล้วด้วย string replace ตรงจุด. Live-test ผ่านเบราว์เซอร์จริงทั้ง 2 ธีม (เปิดการ์ด BMC ที่บันทึกไว้จริง เช็ค gradient/SWOT/card สีด้วย getComputedStyle) ไม่มี console error. **code-reviewer เจอ+แก้บั๊กจริงเพิ่ม 1 จุด**: `dark:from-primary-950/40` ×2 จุด (การ์ด brand_voice "Archetype/Tone" + competitor_analysis "White Space") ใช้ shade `950` ที่ `tailwind.config.js` ไม่มีจริงใน custom `primary` scale (มีแค่ 50-900) — ซ้ำ pattern บั๊กเดิมจาก Phase H — แก้เป็น `dark:from-primary-900/40` ยืนยันด้วย getComputedStyle จริง (`rgba(30,58,138,0.4)` = primary-900 ตรง config). code-reviewer ยัง generate+save ข้อมูลจริงเพิ่มอีก 4 เครื่องมือ (pain_generator, objection_handler, hook_library, competitor_analysis) เพื่อทดสอบ branch อื่นที่ไม่มีข้อมูลบันทึกไว้ก่อน — ผ่านทั้ง 2 ธีมหมด ไม่มี console error. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |
| **S. Developer pages** | `developers/+page.svelte` (231), `developers/docs/+page.svelte` (317) | 548 | ✅ เสร็จแล้ว (dark mode เท่านั้น — ไม่มี AI prompt ในหน้านี้). Palette เล็ก (dark/primary/amber/green/red) เหมือนหน้า login/register ที่เคยทำ. Live-test จริงผ่านเบราว์เซอร์: สร้าง MCP token จริง เช็ค modal reveal (input/code block/ปุ่มคัดลอก) ในโหมดมืด, เพิกถอน token ทดสอบทิ้ง, เปิดหน้าคู่มือ MCP เช็ค error-code badge (amber/red) ด้วย getComputedStyle — สีตรงตาม config ทั้งหมด ไม่มี console error. Gradient logo icon (from-primary-500 to-primary-700) เป็น badge สีทึบตัวหนังสือขาว ตั้งใจไม่แปลงตาม convention เดิม. **code-reviewer เจอ+แก้บั๊กจริง 1 จุด (บั๊กใหม่ที่ไม่เคยเจอมาก่อน)**: class `dark:text-dark-100/50` ถูกใส่ซ้ำกัน 2 ครั้งใน class attribute เดียวกัน (10 จุดรวม 2 ไฟล์) จากสคริปต์ bulk-regex ที่ผมเขียนเอง — ไม่กระทบสายตา (เบราว์เซอร์ apply class ซ้ำแล้วไม่มีผลต่างจากใส่ครั้งเดียว) แต่เป็นโค้ดที่ไม่สะอาดตาม convention เดิม — แก้ลบซ้ำหมดแล้ว ยืนยันด้วย script ตรวจซ้ำว่าไม่เหลือจุดไหนแล้ว. code-reviewer ยัง live-test เพิ่มเติมครบทั้ง 6 tool-reference card + FAQ section ด้วย getComputedStyle (ที่ผมไม่ได้ scroll ไปเช็คเอง) ทั้ง 2 ธีม ผ่านหมด. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |
| **T. Admin console** | `admin/+page.svelte` | 554 | ✅ เสร็จแล้ว (dark mode เท่านั้น — internal-only ไม่มี AI prompt). Palette เล็ก (dark/primary/amber/green/red) เหมือนหน้า developers. **เจอ+แก้เอง 1 จุด**: tab-underline indicator (`border-b-2 border-primary-600`) ที่ active tab ขาด `dark:border-primary-600` — แก้แล้วก่อนส่งรีวิว. Live-test จริงผ่านเบราว์เซอร์ทั้ง 3 แท็บ (ภาพรวม/Users/Email Outbox) + เปิด user detail modal จริง (role dropdown, credit adjust, credit history สีเขียว/แดง) ทั้ง 2 ธีม — ไม่มี console error. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |
| **U. Billing + Demo** | `billing/+page.svelte` (316), `demo/+page.svelte` (476) | 792 | ✅ เสร็จแล้ว. **`billing/+page.svelte`**: แปลง dark mode ครบ (dark/primary/green/red/amber/yellow/gray palette). เจอ+แก้เอง 2 จุด: gradient credits-badge ในการ์ดแพ็กเกจ (`from-blue-50 to-purple-50`) ขาด `dark:from-/to-`, และ border ของการ์ด popular/hover ขาด `dark:border-primary-400`/`dark:hover:border-primary-700`. Live-test จริงผ่านเบราว์เซอร์: เปิด payment modal จริง (mock PromptPay QR) ทั้ง 2 ธีม ไม่มี console error. **`demo/+page.svelte`**: **ตั้งใจไม่แปลง** — เป็นหน้า self-running marketing demo (public, ไม่ผ่าน login) ที่มี aesthetic คงที่ของตัวเอง (พื้นหลัง gradient slate-900→purple-900 + ตัวหนังสือขาวเสมอ, การ์ด preview เนื้อหาเป็นพื้นขาวเสมอเพื่อจำลอง "ภาพหน้าจอ" ของเครื่องมือ AI) ไม่เคยใช้ class `dark:` เลยแม้แต่จุดเดียว และไม่ตอบสนองต่อ theme toggle ของแอปอยู่แล้วโดยดีไซน์ — เหมือน landing page ในทางที่หน้านี้เป็น marketing surface ไม่ใช่ themed app UI ที่ user ล็อกอินแล้วเห็น. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |
| **V. Landing page** | `routes/+page.svelte` | 369 | ✅ เสร็จแล้ว — **เฟสสุดท้ายของแผน dark mode rollout ทั้งหมด**. ต่างจาก `/demo` (Phase U) ตรงที่หน้านี้ใช้ pattern มาตรฐานเดียวกับทุกหน้าที่แปลงมาแล้ว (bg-white card, text-dark-900/NN opacity, bg-dark-50 section) ไม่ใช่ fixed-aesthetic artifact — จึงแปลงแบบเต็มเหมือน login/register/tools ไม่ใช่ข้ามแบบ demo. **เจอ+แก้บั๊กจริงเอง 2 จุดสำคัญ** ที่เจอจาก live-test จริง ไม่ใช่แค่ script scan: (1) opacity-suffix corruption ซ้ำ pattern เดิมบน `bg-white/80` (nav backdrop) — regex ทั่วไปจับ "bg-white" ก่อนจุดที่มี "/80" ทำให้ opacity หลุดไปติดฝั่ง dark: แทน แก้เป็น `bg-white/80 dark:bg-dark-800/80`; (2) **บั๊กจาก context ผิด** ที่ script ไม่มีทางจับได้เอง — ปุ่ม "สมัคร" สีขาวทึบบนการ์ด Pro pricing (gradient สีทึบ `from-primary-500 to-primary-700` ที่ไม่แปลง dark: อยู่แล้ว) และปุ่ม "เข้าสู่ระบบ" โปร่งแสงขาวบนการ์ด waitlist CTA (gradient มืดถาวร `from-dark-900 to-dark-950`) ทั้งสองปุ่มถูก regex ทั่วไปเผลอใส่ `dark:` ให้ทั้งที่นั่งอยู่บนการ์ดที่ไม่แปลง dark: อยู่แล้ว ทำให้สีปุ่มเพี้ยนในโหมดมืด (กลายเป็นพื้นเข้มซ้อนพื้นเข้ม) — ต้อง revert กลับเป็น class เดิมไม่มี dark: เลย. Live-test จริงผ่านเบราว์เซอร์ครบ: hero, pricing (การ์ด Pro + ปุ่มขาว), waitlist CTA (ปุ่มโปร่งแสง), FAQ, footer ด้วย getComputedStyle ทั้ง 2 ธีม ยืนยันปุ่มพิเศษ 2 จุดข้างต้นยังคงสีเดิม (`rgb(255,255,255)` / `rgba(255,255,255,0.1)`) ไม่เปลี่ยนตามธีม ตามที่ตั้งใจ. Verify ครบ: typecheck (54-error web/41-error api baseline เดิม) + build + vitest 38/38 |

รวมทั้งหมด ~10,940 บรรทัด (11 เฟสย่อย L-V) หลัง K เสร็จแล้ว — แต่ละเฟสตาม pattern เดิม: implement → typecheck+build → live-verify ทั้ง 2 ธีม → code-reviewer → update §5 + memory → commit+push+deploy ก่อนเริ่มเฟสถัดไป

> เอกสารนี้เป็น **แผน/บรีฟ** สำหรับ designer + เฟส implement — ยังไม่มีการแก้โค้ด UI จริงในรอบนี้
