# Changelog

สรุปการเปลี่ยนแปลงของ Business Smart OS ตามแผน [`docs/OS_REDESIGN_MASTER_PLAN.md`](docs/OS_REDESIGN_MASTER_PLAN.md)
และ [`docs/CONTENT_PLAYBOOK_UPGRADE_PLAN.md`](docs/CONTENT_PLAYBOOK_UPGRADE_PLAN.md) — เรียงจากล่าสุดไปเก่าสุด

ทุกงานผ่านการทดสอบสดกับข้อมูลจริง (ไม่ใช่ mock) และตรวจซ้ำโดย `code-reviewer` ก่อน commit เสมอ

---

## 2026-08-10 — Stage F: ขัดเงารอบนอก (ปิดแผน OS_REDESIGN_MASTER_PLAN.md ทั้งฉบับ)

หัวใจของ Stage นี้: ฟีเจอร์หลักทำครบแล้วตั้งแต่ Stage A-E เหลือแค่ขัดผิวรอบนอกให้สมบูรณ์

- **F1 — Landing page ความซื่อสัตย์**: แก้ 6 จุดที่ตัวเลข/คำโฆษณาไม่ตรงระบบจริง — จำนวนเครื่องมือ (10→11), แพ็กเกจราคาขาดตัวถูกสุด (เพิ่ม Starter ฿99), ปุ่มลิงก์ผิดเป็น mailto, คำโฆษณา "Export PDF, Word, Excel" เกินจริง (ของจริงมีแค่ Word/CSV/JSON/Markdown + พิมพ์เป็น PDF เองจากเบราว์เซอร์), เรตเครดิต≈บาทใน `CostLine.svelte` คำนวณผิด, comment เก่าผิด
- **F2 — ปฏิทินมือถือ**: เดิมย้ายวันโพสต์บนมือถือทำไม่ได้เลย (ลากวางแบบ HTML5 ใช้นิ้วไม่ได้) — แก้โดยแตะวันเปิด bottom sheet แตะรายการเปิดหน้าแก้ไขที่มีปุ่ม "เปลี่ยนวัน" อยู่แล้ว; เพิ่มจุดสีสถานะ (พร้อมโพสต์/ตั้งเวลาไว้/โพสต์แล้ว) แบบ semantic token
- **F3 — Admin Operations Console**: เปลี่ยนหน้า Admin จากตารางดิบ + modal มือ + emoji ให้ใช้ชุด component เดียวกับทั้งระบบ (Card/Chip/ListItem/Sheet จริง/Icon), เปลี่ยน `alert()`/`confirm()` เป็น toast + inline confirm; เจอบั๊กเก่าไปด้วย (กราฟผู้สมัครใหม่ 14 วันไม่เคยแสดงเลยตั้งแต่สร้าง) แก้ไปพร้อมกัน
- **F4 — ขั้นที่ 7 รีดสื่อ (Repurpose)**: ปุ่ม "🔁 แปลงเป็นสื่ออื่น" บนโพสต์ที่อนุมัติ/ตั้งเวลา/โพสต์แล้ว แปลงเป็นอัลบั้มภาพ / สคริปต์ Reels-TikTok / ข้อความ LINE broadcast โดยใช้ hook/caption/cta ของโพสต์ต้นฉบับตรงๆ (ไม่ใช่แค่หัวข้อ) ห้ามแต่งข้อเท็จจริงเพิ่ม — ผลลัพธ์เข้าคิว Inbox เดิมทันที
- **โครงสร้างพื้นฐาน (นอกแผน แต่แก้ต้นเหตุจริง)**: `scripts/deploy-web.sh` ลบ cache เก่าก่อน build ทุกครั้ง (กันบั๊กหน้าใหม่หายไปเงียบๆ ทั้งที่ deploy สำเร็จ) + เพิ่ม `scripts/smoke-test.sh` เช็ค 8 จุดหลักอัตโนมัติหลัง deploy ทุกครั้ง ผูกเข้ากับ `npm run deploy` ทุกแอปแล้ว

## 2026-08-09 — Stage E: ร้านของฉัน + Editor ใหม่

- **E1 — ร้านของฉัน**: หน้าใหม่รวมทุกอย่างของร้านไว้ที่เดียว — ข้อมูลร้าน/แผนร้าน/คลังรูป/เครดิต/ตั้งค่า (เดิมกระจาย 6 หน้า)
- **E2+E3 — Editor ใหม่**: เปลี่ยนจากฟอร์ม 7 ช่องเป็น "เห็นโพสต์เสร็จ → จิ้มส่วนที่อยากแก้" + ปุ่ม✨บอกทิศ (เลือกโทน/อารมณ์ก่อนให้ AI เขียนใหม่ — ตัดเหลือ enumerated chip กันเลือกยากเกินไป)
- **E4 — Checklist ก่อนโพสต์**: เช็คลิสต์ 6 ข้อในหน้าแก้โพสต์ — AI เช็คให้ 4 ข้อ (พาดหัวไอเดียเดียว/CTA เดียว/ข้อมูลตรงจริง/ตรงเสียงแบรนด์) ผ่านปุ่ม "✨ ตรวจ 60 วิ", อีก 2 ข้อเช็คเอง; ปิด ขั้นที่ 6 ของแผน content ครบสมบูรณ์

## 2026-08-09 — Stage D: Content ขั้น 4-5

- **D1 — Framework Engine**: 5 สูตรโพสต์ (How-to, เล่าเรื่อง 5 จังหวะ, Mindset 3 แบบ) หมุนใช้ตาม pillar อัตโนมัติในโค้ด ไม่ใช่ให้ AI เลือกเอง, เลิกกฎความยาว "3-5 บรรทัด" ตายตัว
- **D2 — โพสต์ขาย 15 ขั้น**: หน้า `/create/sales` ฟอร์มเติมข้อเท็จจริงล้วน (ราคา/โปร/การันตี/ช่องทาง) + ปุ่ม "ปิดจบแบบขายเนียนๆ" ต่อท้ายโพสต์อื่นได้ทุกประเภท
- **D3 — เสียงแบรนด์**: ตั้งค่า ครับ/ค่ะ ระดับแบรนด์ ฉีดเข้าทุกจุดที่ AI เขียนผ่าน `formatBrandContextBlock` ที่เดียว + golden-set 10 แบรนด์จริงคุม regression

## 2026-08-09 — Stage C: วันนี้ + สร้าง (เส้นทางป้าน้อย)

หน้า "วันนี้" ใหม่เป็นหน้าแรกของระบบ (บรีฟรายวัน แทน dashboard เดิมที่มี 4 CTA แข่งกัน), เส้นทางสร้างโพสต์แบบถามคำถามเดียว "วันนี้อยากโพสต์เรื่องอะไร", สลับเมนูจริงทั้งเว็บให้ bottom nav ชี้หน้าใหม่ + redirect ทุกเส้นทางเดิม, แสดงต้นทุนเครดิตก่อนกดทุกปุ่มที่เสียเครดิต

## 2026-08-07 — Content ขั้นที่ 2: ธีมเสาหลัก + Topic Picker (ปิด Stage B)

Taxonomy คอนเทนต์ 14 ประเภท (4 กลุ่ม: ขายฝัน/Expert/Engagement/Review), ปฏิทินการตลาดไทย 16 เทศกาลแบบ static, AI เสนอธีม 3-5 อันให้เลือก → คิดหัวข้อ 8-12 ใบต่อธีม → แตะเลือกป้อนเข้าตัวสร้างโพสต์เดิม — แทนที่การพิมพ์หัวข้อเองทั้งหมด

## 2026-08-06 — Content ขั้นที่ 1: Brand Bootstrap + Stage A: รากฐานดีไซน์

- **Brand Bootstrap**: สร้างโปรไฟล์แบรนด์ + ลูกค้าตัวแทน 1 คนแบบเจาะจง (ต้องมีคำบ่นจริง 3 ข้อ) ผ่าน 2 ทางเข้า (wizard เดิม หรือตอบ 3 คำถามให้ AI ร่างให้) — แก้บั๊กจริงที่ปุ่ม✨เขียนใหม่ไม่เคยรู้จักแบรนด์เลยไปด้วย
- **Stage A — รากฐานดีไซน์**: token สีอุ่นครีม/ดินเผา + type scale 6 ขั้น, component canon 10 ชิ้น (Card/Button/ListItem/TapCard/Chip/Sheet/Field/EmptyState/Toast+Undo/CostLine) ที่ทุกหน้าใหม่หลังจากนี้ใช้ร่วมกัน, ไอคอนวาดเองแทน emoji, bottom nav 4 ที่ (วันนี้/สร้าง/ปฏิทิน/ร้านของฉัน), ปฏิรูปภาษาไทยล้วนทั่วเว็บ ~27 ไฟล์

---

## สถานะ ณ ตอนนี้

**แผน OS_REDESIGN_MASTER_PLAN.md เสร็จสมบูรณ์ครบทุก Stage (A-F) และขึ้นโปรดักชันแล้วที่ `https://businessaios-web.pskspace.workers.dev`**

ดูรายละเอียดเชิงลึกของแต่ละ milestone (บั๊กที่เจอ, สิ่งที่ทดสอบสดจริง, สิ่งที่ code-reviewer แก้) ได้ที่ตาราง "สถานะงาน" ท้าย [`docs/OS_REDESIGN_MASTER_PLAN.md`](docs/OS_REDESIGN_MASTER_PLAN.md) และ [`docs/CONTENT_PLAYBOOK_UPGRADE_PLAN.md`](docs/CONTENT_PLAYBOOK_UPGRADE_PLAN.md)
